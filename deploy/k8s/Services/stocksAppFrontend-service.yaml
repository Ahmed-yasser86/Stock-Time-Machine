apiVersion: v1
kind: Service
metadata:
  name: stocksapp-frontend-service
spec:
  type: NodePort
  selector:
    app: stocksapp-frontend
  ports:
    - protocol: TCP
      port: 81
      targetPort: 80
